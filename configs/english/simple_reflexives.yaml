exp: MinimalPair

mode: 
    - evaluate
    - analyze

models:
    hf_causal_model:
      - mbert-base-multilingual-cased

predfpath: ./predictions/pred_simple_reflexives.tsv
datafpath: ./test_cases/eval_tsv_files/english/simple_reflexives.tsv
resultsfpath: ./results/results_simple_reflexives.tsv
save: by_word, by_pair, by_cond
